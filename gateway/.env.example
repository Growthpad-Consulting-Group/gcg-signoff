# ─── Gateway listener ───────────────────────────────────────────────────────
GATEWAY_SMTP_HOST=0.0.0.0
GATEWAY_SMTP_PORT=2525
# Comma-separated IP allowlist. Leave empty for local dev; MUST be set to Google's published
# outbound-gateway IP ranges before pointing production Workspace routing here.
GATEWAY_ALLOWED_IPS=
# Set to "true" to log the final message instead of actually relaying it — safe for local testing.
GATEWAY_DRY_RUN=true
# Real TLS cert for STARTTLS (Let's Encrypt) — leave both blank to fall back to smtp-server's
# default cert, whose private key is publicly known (fine for local dev, NOT for anything
# reachable from the internet). See README.md for the certbot + deploy-hook setup.
GATEWAY_TLS_CERT_PATH=
GATEWAY_TLS_KEY_PATH=

# ─── Signoff app (the Next.js app in the repo root) ────────────────────────
RENDER_API_URL=http://localhost:3000/api/render
RENDER_API_SECRET=

# ─── Outbound delivery ──────────────────────────────────────────────────────
# This gateway now performs direct delivery to recipient MX servers (outbound MTA).
# No relay service configuration needed.
